# Scarabble — инструкции для Claude

## Конфигурация

Все конфигурационные данные (числа, длительности, коэффициенты, пороги) должны помещаться в `conf.lua`, а не задаваться локальными константами в других файлах.
